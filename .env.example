# ds-proxy 环境变量模板
#
# 使用方式（任选其一）：
#   1. 直接运行 python proxy.py --config config.yaml
#      首次启动未检测到 key 时会交互式询问，并自动生成 .env
#   2. 手动复制本文件为 .env 后填入：
#        cp .env.example .env
#     然后编辑 .env，把下面的 key 填上
#
# DeepSeek API Key（必填）
# 获取地址：https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=

# 上游地址不用在这里配置——它由 config.yaml 的 upstream.base 控制（默认 https://api.deepseek.com）
